# Local developer example — copy to .env (never commit .env)
# DATABRICKS_HOST is not a secret; tokens must never be committed.

DATABRICKS_HOST=https://adb-xxxxxxxx.azuredatabricks.net
# DATABRICKS_TOKEN=  # use `databricks auth login` instead of storing a PAT when possible
BUNDLE_TARGET=dev
